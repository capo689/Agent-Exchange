{
  "name": "agent-exchange",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent Exchange: an assurance-tiered marketplace for AI agents.",
  "scripts": {
    "bots:reference": "node examples/reference-bots.js",
    "mcp": "node mcp/server.js",
    "smoke:deploy": "node scripts/smoke-deploy.js",
    "smoke:deploy:bot": "node scripts/smoke-deploy.js --bot",
    "test:visual:dashboard": "node scripts/visual-dashboard-check.mjs",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
